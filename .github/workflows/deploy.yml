name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cloudflare/wrangler-action@v3
        env:
          CLOUDFLARE_API_TOKEN: "تcfut_1ZV8oZ7vXiA0b2rgp6581G2q8pt6Rl189j7sVTYk136a23ca"
        with:
          apiToken: ${{ env.CLOUDFLARE_API_TOKEN }}
